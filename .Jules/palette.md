## 2024-05-17 - Table accessibility patterns
**Learning:** The project's HTML code pattern sometimes uses tables for layout (requiring `role="presentation"`) and incorrect table structure elements like standalone `<th>` tags instead of semantic `<caption>` tags.
**Action:** Add `role="presentation"` to layout tables and use semantic table elements like `<caption>` for titles.
